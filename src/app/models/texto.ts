export class Texto {
  source: string;
  input: string;
  output: string;
  target: string;

  Texto(source?: string, input?: string, output?: string, target?: string){
    this.source = source;
    this.input = input;
    this.output = output;
    this.target = target;
  }
}
